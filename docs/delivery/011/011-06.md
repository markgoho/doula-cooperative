# 011-06 Add JSON-LD structured data to doula profile and list pages

## Description

Implement JSON-LD schema (e.g., Person, LocalBusiness) in Hugo templates for doula profile and taxonomy list pages to enhance SEO and enable rich search result features.

## Status History

| Timestamp           | Event Type    | From Status | To Status | Details           | User |
| ------------------- | ------------- | ----------- | --------- | ----------------- | ---- |
| YYYY-MM-DD HH:MM:SS | Status Change | Proposed    | Proposed  | Task file created | AI   |

## Requirements

- Add appropriate JSON-LD structured data to doula profile pages (e.g., Person, LocalBusiness schema).
- Add summary/aggregate JSON-LD (e.g., ItemList) to taxonomy list pages if beneficial.
- Use Hugo’s templating to inject JSON-LD into the `<head>` or appropriate place in the HTML.
- Ensure all required and recommended fields are populated from front matter or site data.
- Validate JSON-LD using Google’s Rich Results Test or similar tools.

## Implementation Plan

1. Research the most appropriate schema types for doulas (Person, LocalBusiness, etc.).
2. Update `layouts/doulas/single.html` to include JSON-LD for each doula profile.
3. Update taxonomy list templates (e.g., `layouts/types/list.html`) to include ItemList or other relevant schema.
4. Map front matter fields to schema properties.
5. Test and validate the output using Google’s Rich Results Test.

## Verification

- Doula profile and list pages include valid, well-formed JSON-LD.
- Rich results are detected in Google’s testing tools.
- No errors or warnings in structured data validation.

## Files Modified

- hugo/layouts/doulas/single.html
- hugo/layouts/types/list.html

[Back to task list](../tasks.md)

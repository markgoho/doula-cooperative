export const STRIPE_SECRETS = ["STRIPE_API_KEY", "STRIPE_WEBHOOK_SECRET"];

/**
 * Stripe API version used by the application.
 * Update this when upgrading the Stripe SDK.
 * Must match the version configured in Stripe Dashboard.
 */
export const STRIPE_API_VERSION = "2025-12-15.clover";
